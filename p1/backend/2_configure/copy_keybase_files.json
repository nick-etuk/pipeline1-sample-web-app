{
  "dependencies": ["install_keybase"],
  "checks": {
    "unix": ["test -n \"$( ls -A \"$HOME/.nhsonline/secrets\" )\""],
    "win": ["wsl -u $WSL_USER -- test -n \"$( wsl -u $WSL_USER -- ls -A \"$HOME/.nhsonline/secrets\" )\""]
  }
}
