{
  "dependencies": ["build_backendworker"],
  "newTab": true,
  "timeout": 300,
  "checks": {
    "debug": true,
    "unix": ["check_service ${args[@]+\"${args[@]}\"}"]
  },
  "options": [{ "key": "LOGINENV", "value": "sandpit" }]
}
