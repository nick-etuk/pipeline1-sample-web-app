{
  "checks": {
    "unix": [
      "adb -s emulator-$ANDROID_EMULATOR_PORT shell cat /system/etc/hosts | grep -q web.local.bitraft.io"
    ],
    "win": [
      "adb -s emulator-$ANDROID_EMULATOR_PORT shell cat /system/etc/hosts | Select-String -Quiet web.local.bitraft.io"
    ]
  }
}
