{
  "dependencies": ["start_backendworker"],
  "newTab": true,
  "timeout": 300,
  "checks": {
    "unix": [
      "curl -s http://web.local.bitraft.io:3000 | grep -iq 'class=\"nhsuk-logo\"'"
    ]
  }
}
